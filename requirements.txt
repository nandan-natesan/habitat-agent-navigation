# Habitat Simulator Intro - Dependencies
# Install habitat-sim separately per official docs (conda recommended)

# Core
numpy>=1.20.0
matplotlib>=3.4.0
seaborn>=0.12.0
Pillow>=8.0.0

# Habitat-Sim (install via conda)
# conda install -c conda-forge habitat-sim

# Optional for Colab
gdown
